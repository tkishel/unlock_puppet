{
  "description": "Unlock puppet agent runs exceeding runinterval or runtimeout",
  "input_method": "stdin",
  "parameters": {
    "force_process": {
      "description": "Force a kill of the puppet agent process, and delete its lock file",
      "type": "Optional[String]"
    },
    "force_service": {
      "description": "Force a restart of the puppet service",
      "type": "Optional[String]"
    }
  },
  "supports_noop": false
}
